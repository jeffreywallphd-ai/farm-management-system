# Local Transcription Models

Whisper model binaries are intentionally not committed to this repository.

The app stores downloaded transcription models in local app-controlled storage on the device. Development and internal test builds should not rely on committed `.bin` model files.
