# Theme

This folder is for future Mobile Pilot 1 visual tokens and styling helpers.

Theme work should serve field usability and accessibility rather than decorative complexity.

Do not select a separate UI framework here without dependency and ADR review if it materially changes the stack.
