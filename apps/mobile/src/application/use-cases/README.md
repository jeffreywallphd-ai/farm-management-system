# Use Cases

This folder is reserved for Mobile Pilot 1 application use cases.

Use cases should express farmer workflow intent and call ports rather than SQLite, Expo FileSystem, or React components directly.

Do not implement voice/photo, server sync, shared publication, authentication, or extra record workflows here without later accepted scope.
