# Schema

This folder is for future SQLite schema definitions or notes used by Mobile Pilot 1 infrastructure adapters.

Schema files must support the accepted Pilot 1 records and local reference data without becoming the sole domain authority.

Do not add server schema, sync protocol schema, or ORM models here.
